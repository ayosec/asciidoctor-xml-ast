#!/bin/bash

IN_REPO=$(realpath "$(dirname "$0")")/../lib/asciidoctor-xml-ast.rb

if [ -f "$IN_REPO" ]
then
  LIB=$IN_REPO
else
  LIB=asciidoctor-ast-xml
fi

exec asciidoctor --require "$LIB" --backend xml-ast "$@"
